package api

const (
	ColdBrewScheduler = "coldbrew-scheduler"
)

const (
	V1 = "v1"
)

const (
	ResourceNode = "Node"
	ResourcePods = "pods"
)

const (
	SpecPrefix        = "spec."
	SpecNodeName      = SpecPrefix + "nodeName"
	SpecSchedulerName = SpecPrefix + "schedulerName"
)
